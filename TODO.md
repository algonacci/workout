# TODO

- [ ] Run `node fetch.mjs` daily to sync activities + photos
- [ ] Refresh Strava token when expired (6h) — re-auth via OAuth URL
- [ ] Set up GitHub Actions cron to auto-run `fetch.mjs` daily
